/*
const edades = [33, 27, 34, 30, 34, 25];
 punto 1:
function bubbleSort(array) {
  for (let x = 0; x < array.length; x++) {
    for (let i = 0; i < array.length - 1; i++) {
      if (array[i] > array[i + 1]) {
        let temporal = array[i];
        array[i] = array[i + 1];
        array[i + 1] = temporal;
      }
    }
  }
}

bubbleSort(edades);
console.log(edades);

punto 2:
function bubbleSortReverse(array) {
  for (let x = 0; x < array.length; x++) {
    for (let i = 0; i < array.length - 1; i++) {
      if (array[i] < array[i + 1]) {
        let temporal = array[i];
        array[i] = array[i + 1];
        array[i + 1] = temporal;
      }
    }
  }
}

bubbleSortReverse(edades);
console.log(edades);

punto 3:


const letras = ["C", "A", "D", "E", "H", "Z", "J", "L"];

function bubbleSortAZ(array) {
  for (let x = 0; x < array.length; x++) {
    for (let i = 0; i < array.length - 1; i++) {
      if (array[i] > array[i + 1]) {
        let temporal = array[i];
        array[i] = array[i + 1];
        array[i + 1] = temporal;
      }
    }
  }
}

bubbleSortAZ(letras);
console.log(letras);
*/

/*
const arrayCuentas = [
    {
      titular: "Arlene Barr",
      estaHabilitada: false,
      saldo: 3253.4,
      edadTitular: 33,
      tipoCuenta: "sueldo"
    },
    {
      titular: "Roslyn Torres",
      estaHabilitada: false,
      saldo: 3229.45,
      edadTitular: 27,
      tipoCuenta: "sueldo"
    },
    {
      titular: "Cleo Lopez",
      estaHabilitada: true,
      saldo: 1360.19,
      edadTitular: 34,
      tipoCuenta: "corriente"
    },
    {
      titular: "Daniel Malone",
      estaHabilitada: false,
      saldo: 3627.12,
      edadTitular: 30,
      tipoCuenta: "sueldo"
    },
    {
      titular: "Ethel Leon",
      estaHabilitada: true,
      saldo: 1616.52,
      edadTitular: 34,
      tipoCuenta: "sueldo"
    },
    {
      titular: "Harding Mitchell",
      estaHabilitada: true,
      saldo: 1408.68,
      edadTitular: 25,
      tipoCuenta: "corriente"
    }
  ];


  */
  /*
  //Punto 3.a:
  
  function bubbleSortSaldo(array) {
    for (let x = 0; x < array.length; x++) {
      for (let i = 0; i < array.length - 1; i++) {
        if (array[i].saldo > array[i + 1].saldo) {
          let temporal = array[i];
          array[i] = array[i + 1];
          array[i + 1] = temporal;
        }
      }
    }
  }
  
  bubbleSortSaldo(arrayCuentas);
  console.log(arrayCuentas);
  
  //punto 3.b
  
  function bubbleSortEdad(array) {
    for (let x = 0; x < array.length; x++) {
      for (let i = 0; i < array.length - 1; i++) {
        if (array[i].edadTitular > array[i + 1].edadTitular) {
          let temporal = array[i];
          array[i] = array[i + 1];
          array[i + 1] = temporal;
        }
      }
    }
  }
  
  bubbleSortEdad(arrayCuentas);
  console.log(arrayCuentas);
  */
  